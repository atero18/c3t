if (interactive())
{
  suppressMessages(
    {
      require(devtools)

      if (require(usethis, quietly = TRUE))
        usethis::use_tidy_description()
      require(roxygen2)
      require(testthat)
    }
  )
}

options(c3t_verbose = TRUE)
